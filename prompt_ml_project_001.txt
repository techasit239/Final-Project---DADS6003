>คุณคือ Expert Data science
>Code ของคุณ Clean และ อ่านเข้าใจง่ายสำหรับมือใหม่
>วัตถุประสงค์ : ต้องการเปรียบเทียบ Evaluation ของทุก Model โดยมีรายละเอียดการทดลอง 3 แบบ ดังนี้
>>Reproduce from paper : Stylometric Feature only (ไม่รวม 8 Advanced Features)
>>New Idea (รวม 8 Advanced Features) ไปด้วย จากนั้นทำออกมา 3 Experiments ดังนี้
>>>Experiment A : Stylometric Feature only !!!
>>>Experiment B : TF-IDF Feature only !!!
>>>Experiment C : Stylometric + TF-IDF features (combines)
>>จากนั้นดูภาพรวมค่าเฉลี่ย Evaluation ของทุก Model ในแต่ละ Experiment --> แล้วเลือก Experiment ที่ค่าเฉลี่ยรวมดีที่สุด โดยใช้ Best  Model ใน Best Experiment (A or B or C) มาทำ Feature selection (Top 10, 20, 30) แล้วนำไปหา Evaluation ของ Best model นั้นเพื่อเปรียบเทียบ Evaluation ของ Feature selection แต่ละแบบ
>โจทย์ : ช่วยแก้ Code ที่แนบให้ (การทำ ML เพื่อจับ Phishing email ที่ Generated จาก AI GPT-4o) โดยให้มีขั้นตอนเหมือนเดิม (Part 0 --> Part 4) แต่ช่วยเขียนโค้ดให้เป็นระเบียบ
ตามลำดับ Flow การทำ ML (พร้อมเขียนว่า แต่ละขั้นตอน เรียกว่าอะไร แล้ว จากขั้นตอนนี้ ต้องไปขั้นตอนไหนต่อ จนจบ Flow
>>โดยมีรายละเอียดที่ต้องมีใน Flow ดังนี้ (ไม่รวมขั้นตอนอื่นๆที่จำเป็นต้องทำอยู่แล้ว)
>>>Training method : CrossValidation method (k=5)
>>>การสร้าง Feature ในการ Train : มีการทำ TF-IDF เพิ่มเข้ามา จากตอนแรกที่มีแค่ Stylometric feature (ตาม paper)
>>>Model ML ที่ต้องการทำ :
Existing model (from paper)
1. LogisticRegression
2. SVM
3. RandomForrest
4. XGBoost
New model 
5. ADABoost
6. CatBoost
7. LightGBM
8. NaiveBayes
9. [Model Classification สำหรับงาน NLP ที่คุณแนะนำ (แต่ต้องไม่มีปัญหา BlackBox นะ!!!)]
>>>การ Tuning hyperparameter โดย Optuna (Tune ทุก Model) พร้อมบอกค่า hyperparameter ที่ดีที่สุด แล้วเอาไปใช้สรุปผลลัพธ์
>>>ผลลัพธ์
โดยต้องการผลลัพธ์แสดงเป็นตาราง df from display() เรียงตามลำดับดังนี้
- ตาราง Evaluation โดยมีรายละเอียด และ ลำดับดังนี้
>>>>Index (start from 1 to last) | Model | Accuracy | Recall | Precision | F1-score | MCC-score | AUC | PR
- ตาราง Confusion Matrix ของแต่ละ Model
>>>>Index (start from 1 to last) | Model | TN | FP | FN | TP
- โดยในแต่ละแบบ ทั้ง Re-Produce (ไม่รวม 8 Advanced Features) และ New Idea (รวม 8 Advanced Features) Experiment (A,B,C) ต้อง แบ่งเป็น 3 ตาราง ตามไฟล์ train.csv (100 rows) / test.csv (26 rows) ตัวอย่างเช่น
>>Reproduce from paper
>>>>ตาราง Evaluation ของ train set (train.csv)
>>>>ตาราง Evaluation ของ validation set (train.csv)
>>>>ตาราง Evaluation ของ test set จริงๆ ที่เป็น unseen data (test.csv)
>>New Idea
>>>Experiment A 
>>>>ตาราง Evaluation ของ train set (train.csv)
>>>>ตาราง Evaluation ของ validation set (train.csv)
>>>>ตาราง Evaluation ของ test set จริงๆ ที่เป็น unseen data (test.csv)
>>>Experiment B
>>>>ตาราง Evaluation ของ train set (train.csv)
>>>>ตาราง Evaluation ของ validation set (train.csv)
>>>>ตาราง Evaluation ของ test set จริงๆ ที่เป็น unseen data (test.csv)
ผลลัพธ์ที่เปรียบเทียบต้องใช้ evaluation จาก test set จริงๆ ที่เป็น unseen data
>>>Experiment C
>>>>ตาราง Evaluation ของ train set (train.csv)
>>>>ตาราง Evaluation ของ validation set (train.csv)
>>>>ตาราง Evaluation ของ test set จริงๆ ที่เป็น unseen data (test.csv)
- ROC & PR curve ของแต่ละ Model ในแต่ละแบบ ( Reproduce / New Idea : Experiment (A,B,C) ) โดยช่วยใช้รูปแบบกราฟที่เข้าใจง่ายที่สุด (มือใหม่ หรือ เด็กอ่านก็เข้าใจ)
- ให้แสดง กราฟ Horizontal bar chart Top 10 features ของ best model ในแต่ละ Experiment (A, B, C) และ best model ที่ดีที่สุด 1 model จากทุก Experiment
- กราฟ Horizontal bar chart Top 10 features ต้องมี data labels บอกค่า % ด้วย
>รายละเอียดอื่นๆดังนี้
>>Run code ใน Colab
>>Stylometric featues ต้องครบตามของเดิม หรือ ครบตาม paper (ประมาณ 60 features)
>>ตอน run ช่วยบอก และ สรุป จำนวน features (จำนวน columns) ทั้งหมดของแต่ละ experiment (A,B,C) ด้วย ตัวอย่าง เช่น (100, 60) หรือ (100, 5000) เป็นต้น โดยอิงตาม features จริงที่ถูกส่งไปใช้ train data
>>Modelที่แนะนำ ต้องไม่ใช่ model ที่มีอยู่แล้ว ไป ผสมกับตัวอื่น ขอเป็นตัวที่มี algorithm ใหม่ไปเลย (ตัวอย่าง เช่น NaiveBayes)
>>แยก Part main ออกมาหลังสุด เพื่อป้องกันการสับสน
>>ขอ Code ตั้งแต่ขั้นตอนแรก จนถึงขั้นตอนแสดงผล Evaluation (Best Model ของ Best Experiment)ของ Feature selection ให้ครบสมบูรณ์เลย
>>ต้องการปรับปรุง Stylometric Features: แทนการใช้ค่า Dummy (1.0) สำหรับฟีเจอร์ Linguistic 23 ตัว ควรใช้เครื่องมือ NLP ที่ซับซ้อนยิ่งขึ้น (เช่น POS Tagging) เพื่อดึงฟีเจอร์เหล่านี้ออกมาจริง เพื่อเพิ่มความแม่นยำของ Experiment A และ C
>>Stylometric features ต้องใช้ค่าจริงเท่านั้น ห้ามใช้ค่า Dummy
>>มีการทดสอบ Overfitting โดยดูจาก Line chart เปรียบเทียบ Cost function (Log Loss) vs พารามิเตอร์ของ Best model แต่ละแบบ (กลุ่ม Gradient Boosting/Iterative Linear Model/Ensemble Trees/Non-iterative Models)
>>ตาราง Evaluation ช่วย Sort Descending ตาม Evaluation Avg รวมของแต่ละ Model เพื่อความง่ายในการอ่าน